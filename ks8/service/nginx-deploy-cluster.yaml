apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-cluster
  labels:
    app: nginx-app-cluster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-app-cluster
  template:
    metadata:
      labels:
        app: nginx-app-cluster
    spec:
      containers:
      - name: nginx-container
        image: nginx:1.7.9
        ports:
        - containerPort: 80
